% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ontology.R, R/zzz-aliases.R
\name{ontology}
\alias{ontology}
\alias{Ontology}
\title{What the ontology graph knows about a term}
\usage{
ontology(term, max_hops = 2, children = TRUE, con = .con())

Ontology(term, max_hops = 2, children = TRUE, con = .con())
}
\arguments{
\item{term}{The word or phrase to look up. Case does not matter.}

\item{max_hops}{How far to walk the synonym links, 1 to 4.}

\item{children}{Set \code{FALSE} to skip the children, which is much cheaper.}

\item{con}{A \code{seqout_connection}. Defaults to the shared REST connection.}
}
\value{
A tibble with one row for the term, one for each synonym and one for
each child. Columns: \code{relation}, \code{name}, \code{xrefs} (a list column of source
CURIEs) and \code{has_children}. The \code{synonym_total}, \code{children_truncated} and
\code{max_hops} attributes report what the server capped.
}
\description{
Looks up one term in the graph used for search expansion.
Returns source IDs, synonyms and direct children.
}
\details{
\code{max_hops} bounds the walk over the synonym links only. Children are always
the direct children of the resulting synonym cluster, at any \code{max_hops}.

A term absent from the graph returns an empty tibble.

Reads REST. The ontology graph is absent from the dump.
}
\examples{
\dontrun{
Ontology("liver")

# The identifiers alone, without the hierarchy query
Ontology("breast cancer", children = FALSE)$xrefs

# Terms one level down that expand further
onto <- Ontology("liver")
onto[onto$relation == "child" & onto$has_children, "name"]
}
}
\seealso{
\code{\link[=seqout_search]{seqout_search()}}, which expands a plain query through this graph.
}
