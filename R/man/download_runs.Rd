% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R, R/zzz-aliases.R
\name{download_runs}
\alias{download_runs}
\alias{DownloadRuns}
\title{Download the read files of an accession}
\usage{
download_runs(
  accession,
  dest_dir = NULL,
  mode = NULL,
  quiet = FALSE,
  con = .con()
)

DownloadRuns(
  accession,
  dest_dir = NULL,
  mode = NULL,
  quiet = FALSE,
  con = .con()
)
}
\arguments{
\item{accession}{Any accession SeqOut holds.}

\item{dest_dir}{Directory to write into. Defaults to the accession.}

\item{mode}{Which copy to take: \code{"fastq"}, \code{"sra"} (NCBI's full-quality
copy) or \code{"sra_lite"} (the same reads with binned quality scores). \code{NULL}
takes the first each run offers, in that order.}

\item{quiet}{Suppress progress messages.}

\item{con}{A \code{seqout_connection}. Defaults to the shared REST connection.}
}
\value{
The paths of the downloaded files, invisibly.
}
\description{
Takes any accession: a run downloads its own files, an experiment or sample
downloads its runs', a series or study downloads every run's.
}
\details{
Not every run is served in every form. Left to itself the function takes the
first copy each run offers, preferring ENA fastq, then NCBI's full-quality
SRA copy, then its \code{lite} copy, and says which it took. Name \code{mode} to insist
on one.

Where the archive publishes a checksum, which it does for every fastq, the
downloaded file is verified against it.
}
\examples{
\dontrun{
download_runs("SRR12012336") # one run
download_runs("SRP267291") # every run of a study
download_runs("SRP150719", mode = "fastq") # insist on ENA fastq
}
}
