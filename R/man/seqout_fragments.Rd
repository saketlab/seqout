% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/counts.R, R/zzz-aliases.R
\name{seqout_fragments}
\alias{seqout_fragments}
\alias{SeqoutFragments}
\title{Fragments files for an ATAC study}
\usage{
seqout_fragments(counts, sample = NULL, download = FALSE)

SeqoutFragments(counts, sample = NULL, download = FALSE)
}
\arguments{
\item{counts}{A \code{seqout_counts} tibble from \code{\link[=seqout_counts]{seqout_counts()}}.}

\item{sample}{Sample accessions to keep. \code{NULL} returns every fragments file
in the study.}

\item{download}{Fetch the files and report where they landed. \code{FALSE}, the
default, only lists them.}
}
\value{
A tibble of \code{sample}, \code{file}, \code{url}, \code{indexed} and \code{path}. \code{path} is
\code{NA} until the file is downloaded.
}
\description{
A fragments file holds one bgzipped BED-like row per ATAC fragment, with
columns \code{chrom}, \code{start}, \code{end}, \code{barcode} and \code{read_support}.
}
\details{
Files download only when \code{download = TRUE}.

GEO rarely holds the \code{.tbi} index Signac wants. Build one after downloading
with \code{Rsamtools::indexTabix(path, format = "bed")}.

That works on a CellRanger fragments file, which is bgzipped and sorted by
coordinate. Some deposits are neither: GSE184462 ships plain-gzipped files
sorted by barcode, and tabix rejects them with "Chromosome blocks not
continuous". Sort and recompress first:
\verb{sort -k1,1 -k2,2n}, then \code{Rsamtools::bgzip()}, then \code{indexTabix()}.
}
\examples{
\dontrun{
counts <- SeqoutListCounts("GSE156478")
SeqoutFragments(counts)

frags <- SeqoutFragments(counts, sample = "GSM5065524", download = TRUE)
Rsamtools::indexTabix(frags$path[1], format = "bed")
}
}
\seealso{
\code{\link[=seqout_counts]{seqout_counts()}} for the matrices, \code{\link[=seqout_counts_files]{seqout_counts_files()}} for a
unit's own files.
}
