% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find.R, R/zzz-aliases.R
\name{geo_search}
\alias{geo_search}
\alias{GeoSearch}
\title{Search GEO series by free text with BAM availability}
\usage{
geo_search(con, query, year_from = NULL, year_to = NULL, limit = 200)

GeoSearch(con, query, year_from = NULL, year_to = NULL, limit = 200)
}
\arguments{
\item{con}{A \code{seqout_connection} from \code{\link[=seqout_connect]{seqout_connect()}}.}

\item{query}{Character. One or more words searched across title, summary,
and overall design (e.g., \code{"PBMC scRNA-seq"}). All words must appear
somewhere in the record (AND logic).}

\item{year_from, year_to}{Optional integer. Filter to series published within
this year range.}

\item{limit}{Maximum number of results. Default 200.}
}
\value{
A tibble with columns:
\describe{
\item{accession}{GEO series accession (GSE…).}
\item{title}{Series title.}
\item{published}{Publication date.}
\item{sra_accession}{Linked SRA study accession(s), comma-separated.
\code{NA} if none.}
\item{has_bam}{\code{TRUE} if the series uploaded BAM files as GEO
supplementary data.}
}
}
\description{
Searches GEO series title, summary, and overall design for the given query
terms (AND logic per word, case-insensitive) using DuckDB without the REST
API. Returns one row per matching series with the linked SRA accession and a
\code{has_bam} flag indicating whether the series deposited BAM files as
supplementary data.
}
\details{
\code{has_bam} reflects supplementary files at the \strong{series level} only. A
larger set of studies deposit BAM files at the \strong{sample level} (individual
GSM records). Use \code{\link[=sample_bam]{sample_bam()}} to check sample-level BAM availability
for a specific series.

BAM availability in the linked SRA record cannot be determined from the
current database because run-level file formats are not stored. To check
whether a given SRP has aligned BAM submissions, use the SRA Run Selector or
the SRA toolkit.
}
\examples{
\dontrun{
con <- seqout_connect()

# Find PBMC scRNA-seq datasets
results <- geo_search(con, "PBMC scRNA-seq")

# Subset to those with BAM files
library(dplyr)
results |> filter(has_bam)

# Also filter to those with a linked SRA accession
results |> filter(!is.na(sra_accession))
}
}
