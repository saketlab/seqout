% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/longread.R, R/zzz-aliases.R
\name{longread_projects}
\alias{longread_projects}
\alias{LongreadProjects}
\title{Studies with a PacBio or Oxford Nanopore experiment, any archive}
\usage{
longread_projects(
  technology = NULL,
  platform = NULL,
  instrument_model = NULL,
  library_strategy = NULL,
  organism = NULL,
  archive = NULL,
  chemistry = NULL,
  assay_l1 = NULL,
  year = NULL,
  has_fastq = NULL,
  has_sra = NULL,
  long_read_only = NULL,
  has_exact_chemistry = NULL,
  single_cell = NULL,
  sort = "n_experiments",
  order = "desc",
  limit = NULL,
  offset = 0,
  con = .con()
)

LongreadProjects(
  technology = NULL,
  platform = NULL,
  instrument_model = NULL,
  library_strategy = NULL,
  organism = NULL,
  archive = NULL,
  chemistry = NULL,
  assay_l1 = NULL,
  year = NULL,
  has_fastq = NULL,
  has_sra = NULL,
  long_read_only = NULL,
  has_exact_chemistry = NULL,
  single_cell = NULL,
  sort = "n_experiments",
  order = "desc",
  limit = NULL,
  offset = 0,
  con = .con()
)
}
\arguments{
\item{technology, platform, instrument_model, library_strategy, organism, archive, chemistry, assay_l1}{Character. Filter to one value from \code{\link[=longread_facets]{longread_facets()}}.}

\item{year}{Integer. Publication year.}

\item{has_fastq, has_sra, long_read_only, has_exact_chemistry}{Logical filters.}

\item{single_cell}{\code{TRUE}/\code{FALSE} to keep only studies with (or without) a
single-cell assay; \code{NULL} (default) leaves both in.}

\item{sort}{One of \code{"n_experiments"}, \code{"n_samples"}, \code{"n_runs"},
\code{"first_published"}, \code{"title"}, \code{"study_accession"}, \code{"organism"}.}

\item{order}{\code{"desc"}, the default, or \code{"asc"}.}

\item{limit}{Maximum studies; \code{NULL} reads all, in pages of up to 200.}

\item{offset}{Number of studies to skip.}

\item{con}{A \code{seqout_connection}. Defaults to the shared REST connection;
a Parquet connection reads the dump.}
}
\value{
A study tibble, with a \code{total} attribute for the filtered count
before \code{limit} cut it.
}
\description{
One row per study; a study mirrored in more than one archive counts once.
\code{long_read_only = TRUE} had no other platform; \code{FALSE} is hybrid; \code{NA} has
no experiment rows to judge by (GEO-only entries). Filter values come from
\code{\link[=longread_facets]{longread_facets()}}.
}
\details{
\code{single_cell} keeps only studies also flagged single-cell
(\code{is_single_cell}). The server has no such parameter, so this filters
locally.
}
\examples{
\dontrun{
# long-read-only datasets
LongreadProjects(long_read_only = TRUE, limit = 100)

# long-read AND single-cell
LongreadProjects(single_cell = TRUE, limit = 100)

# Oxford Nanopore human studies, newest first
LongreadProjects(
  technology = "Oxford Nanopore", organism = "Homo sapiens",
  sort = "first_published"
)
}
}
\seealso{
\code{\link[=longread_summary]{longread_summary()}}, \code{\link[=longread_facets]{longread_facets()}},
\code{\link[=project_longread_chemistry]{project_longread_chemistry()}} for one study's per-run detail.
}
