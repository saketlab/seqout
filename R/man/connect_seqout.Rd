% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{connect_seqout}
\alias{connect_seqout}
\title{Connect to the SeqOut database}
\usage{
connect_seqout(base_url = "https://seqout.org", read_only = FALSE)
}
\arguments{
\item{base_url}{Base URL of the SeqOut server. Defaults to
\code{"https://seqout.org"}.}

\item{read_only}{If \code{TRUE}, the DuckDB connection is read-only.
Defaults to \code{FALSE} so that \code{\link[=sq_cache_table]{sq_cache_table()}} can materialise views
locally.}
}
\value{
A \code{seqout_connection} object (list) containing the DuckDB
connection handle and configuration.
}
\description{
Creates a DuckDB connection with remote Parquet views registered for all
SeqOut tables. Uses DuckDB's httpfs extension to query Parquet files hosted
on seqout.org without downloading them entirely.
}
\examples{
\dontrun{
con <- connect_seqout()
sq_query(con, "SELECT * FROM geo_series LIMIT 5")
close_seqout(con)
}
}
