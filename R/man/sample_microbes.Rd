% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pentimento.R, R/zzz-aliases.R
\name{sample_microbes}
\alias{sample_microbes}
\alias{SampleMicrobes}
\title{Microbial reads found in a sample}
\usage{
sample_microbes(
  accession,
  kind = c("all", "viral", "bacterial", "both"),
  min_breadth = NULL,
  min_kmer_mass = NULL,
  validated_only = FALSE,
  include_background = FALSE,
  detail = FALSE,
  limit = 500,
  con = .con()
)

SampleMicrobes(
  accession,
  kind = c("all", "viral", "bacterial", "both"),
  min_breadth = NULL,
  min_kmer_mass = NULL,
  validated_only = FALSE,
  include_background = FALSE,
  detail = FALSE,
  limit = 500,
  con = .con()
)
}
\arguments{
\item{accession}{A GSM, or the archive sample accession behind one.}

\item{kind}{Detection class: \code{"viral"}, \code{"bacterial"}, \code{"both"} or \code{"all"}.}

\item{min_breadth}{Keep only detections covering at least this fraction of
the reference, overriding the stored gate.}

\item{min_kmer_mass}{Keep only detections at or above this weighted k-mer
mass.}

\item{validated_only}{Keep only detections that pass the stored gates.}

\item{include_background}{Include reagent kitome and skin flora, which are
excluded by default.}

\item{detail}{Return per-run rows; the organism view stays on the
\code{by_organism} attribute and costs no second request.}

\item{limit}{The maximum number of detection rows to read, at most 1000.}

\item{con}{A \code{seqout_connection}. Defaults to the shared REST connection;
pass a Parquet one to read the dump instead.}
}
\value{
An organism tibble, or a run-by-organism tibble when
\code{detail = TRUE}. \code{measurable}, \code{n_runs}, \code{totals}, \code{by_kingdom},
\code{control_kingdoms} and the unpicked view are carried as attributes.
}
\description{
Returns one row per organism, ordered by k-mer mass.
}
\details{
Panel-unitig alignments lack raw-read, UMI and replicate confirmation and are
not diagnostic. \code{measurable} of \code{FALSE} or \code{NA} means the sample was never
screened, and the function warns when that happens.

\code{kmer_mass} weights k-mers within a run, so it carries no absolute scale.
\code{n_unitigs} counts mapped unitigs.

\code{spike_in_control} (PhiX) and \code{negative_control} (the papillomavirus that
calibrates the viral threshold) are never summed into the viral or bacterial
totals; \code{bacterial_background} is excluded unless \code{include_background} is set.
}
\examples{
\dontrun{
m <- sample_microbes("GSM5155196", kind = "viral")
m[, c("organism", "n_unitigs", "max_breadth_frac")]
attr(m, "measurable")
attr(m, "detections")
}
}
\seealso{
\code{\link[=project_single_cell]{project_single_cell()}} for the study-wide flags.
}
