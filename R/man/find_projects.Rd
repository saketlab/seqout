% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find.R, R/zzz-aliases.R
\name{find_projects}
\alias{find_projects}
\alias{FindProjects}
\title{Find projects by keywords and filters}
\usage{
find_projects(
  con,
  keywords = NULL,
  organism = NULL,
  assay = NULL,
  source = NULL,
  country = NULL,
  year_from = NULL,
  year_to = NULL,
  min_samples = NULL,
  min_citations = NULL,
  sort_by = "relevance",
  limit = 100
)

FindProjects(
  con,
  keywords = NULL,
  organism = NULL,
  assay = NULL,
  source = NULL,
  country = NULL,
  year_from = NULL,
  year_to = NULL,
  min_samples = NULL,
  min_citations = NULL,
  sort_by = "relevance",
  limit = 100
)
}
\arguments{
\item{con}{A \code{seqout_connection} from \code{\link[=seqout_connect]{seqout_connect()}}.}

\item{keywords}{Optional character. Search terms matched against project
titles and descriptions (case-insensitive). Multiple words are split
and each must appear (AND logic), e.g. \code{"liver cancer scRNA"} finds
projects containing all three terms.}

\item{organism}{Optional character. Scientific name (e.g.,
\code{"Homo sapiens"}, \code{"Mus musculus"}). Use \code{\link[=list_organisms]{list_organisms()}} to
discover valid values.}

\item{assay}{Optional character. Assay type to filter by. Matched against
both broad categories (e.g., \code{"Transcriptomic"}) and specific assays
(e.g., \code{"RNA-seq"}, \code{"ChIP-seq"}). Use \code{\link[=list_assays]{list_assays()}} to discover
valid values.}

\item{source}{Optional character. Database source: \code{"geo"}, \code{"sra"},
\code{"ena"}, or \code{"ae"} (ArrayExpress).}

\item{country}{Optional character. Country name (e.g.,
\code{"United States"}, \code{"China"}). Use \code{\link[=list_countries]{list_countries()}} to discover
valid values.}

\item{year_from, year_to}{Optional integer. Filter by publication year range.}

\item{min_samples}{Optional integer. Minimum number of samples.}

\item{min_citations}{Optional integer. Minimum citation count.}

\item{sort_by}{How to sort results: \code{"relevance"} (default, by citation
count), \code{"date"} (newest first), \code{"samples"} (most samples first),
or \code{"citations"}.}

\item{limit}{Maximum number of results to return. Default 100.}
}
\value{
A tibble with columns: \code{accession}, \code{source}, \code{title},
\code{organism}, \code{assay}, \code{country}, \code{published}, \code{n_samples},
\code{citation_count}.
}
\description{
Search and filter projects across all databases without writing SQL.
Combines free-text keyword matching with structured filters on organism,
assay type, year, country, and more.
}
\examples{
\dontrun{
con <- seqout_connect()

# Find human RNA-seq cancer projects
find_projects(con,
  keywords = "cancer", organism = "Homo sapiens",
  assay = "RNA-seq"
)

# Recent mouse ChIP-seq with many samples
find_projects(con,
  organism = "Mus musculus", assay = "ChIP-seq",
  year_from = 2022, min_samples = 20, sort_by = "date"
)

# Highly cited single-cell projects
find_projects(con,
  keywords = "single cell",
  min_citations = 100, sort_by = "citations"
)
}
}
