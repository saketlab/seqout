% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R, R/zzz-aliases.R
\name{search_facets}
\alias{search_facets}
\alias{SearchFacets}
\title{Count the matches of a search}
\usage{
search_facets(
  query,
  ...,
  structured = FALSE,
  exclude_ontology = NULL,
  con = .con()
)

SearchFacets(
  query,
  ...,
  structured = FALSE,
  exclude_ontology = NULL,
  con = .con()
)
}
\arguments{
\item{query}{Character. The text to count the matches of. Required.}

\item{...}{Filters, by name, to count \emph{within}: \code{db}, \code{organism}, \code{country},
\code{library_strategy}, \code{library_source}, \code{instrument_model}, \code{platform},
\code{journal}, \code{multi_platform}, \code{year_from} and \code{year_to}.}

\item{structured}{Read \code{query} as a boolean expression, as \code{\link[=seqout_search]{seqout_search()}}.}

\item{exclude_ontology}{Ontologies to keep out of the query expansion. See
\code{\link[=seqout_search]{seqout_search()}}.}

\item{con}{A \code{seqout_connection}. Defaults to the shared REST connection;
a Parquet connection reads the dump.}
}
\value{
A tibble of \code{facet}, \code{value}, \code{count} and \code{score}, with \code{total} and
\code{max_rank} attributes.
}
\description{
Groups the full \code{\link[=seqout_search]{seqout_search()}} match set by facet.
}
\details{
Values order by \code{score}, the summed match rank. \code{score} is 0 without a query.
}
\examples{
\dontrun{
f <- SearchFacets("liver cancer")
attr(f, "total")

# organism facet
f[f$facet == "organism", ]

# count within a narrower set
SearchFacets("liver cancer", organism = "Homo sapiens")
}
}
\seealso{
\code{\link[=seqout_search]{seqout_search()}} for the results themselves, \code{\link[=search_suggest]{search_suggest()}}
when a query returns nothing.
}
