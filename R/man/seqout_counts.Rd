% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/counts.R, R/zzz-aliases.R
\name{seqout_counts}
\alias{seqout_counts}
\alias{SeqoutCounts}
\title{Read a GEO accession as a counts matrix}
\usage{
seqout_counts(
  con,
  accession,
  assay = "rna",
  feature_type = NULL,
  cache_dir = NULL
)

SeqoutCounts(
  con,
  accession,
  assay = "rna",
  feature_type = NULL,
  cache_dir = NULL
)
}
\arguments{
\item{con}{A \code{seqout_connection} from \code{\link[=seqout_connect]{seqout_connect()}}.}

\item{accession}{A GSE or GSM accession.}

\item{assay}{Which assay to take when a sample carries several, as CITE-seq
and multiome studies do. Defaults to \code{"rna"}; \code{"adt"}, \code{"hto"} and
\code{"atac"} are recognised too. \code{NULL} takes whichever comes first.}

\item{feature_type}{10x feature class to keep, overriding the one implied by
\code{assay}. \code{NULL} keeps every row.}

\item{cache_dir}{Where downloads land. Defaults to a per-accession directory
under \code{\link[tools:R_user_dir]{tools::R_user_dir()}}, reused across sessions.}
}
\value{
A \code{seqout_counts} handle. Pass it to \code{\link[=manifest]{manifest()}}, \code{\link[=seqout_matrix]{seqout_matrix()}}
or \code{\link[=matrices]{matrices()}}.
}
\description{
Resolves the supplementary files of a series or sample and groups them into
readable units, downloading nothing. GEO payloads run to tens of gigabytes,
so the manifest comes first and costs only the calls that list the files.
}
\details{
A unit is a group of files that read as one matrix: a 10x triplet
(\code{matrix.mtx} with \code{barcodes.tsv} and \code{features.tsv}), a CellRanger \code{.h5}, an
\code{.h5ad}, an \code{.rds}, or a delimited table.
}
\examples{
\dontrun{
con <- seqout_connect()
counts <- seqout_counts(con, "GSE297547")
manifest(counts)
m <- seqout_matrix(counts, sample = "GSM8994520")
}
}
