% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find.R, R/zzz-aliases.R
\name{assay_projects}
\alias{assay_projects}
\alias{AssayProjects}
\title{Get projects by assay type with publication dates}
\usage{
assay_projects(assay, year_from = NULL, year_to = NULL, con = .con())

AssayProjects(assay, year_from = NULL, year_to = NULL, con = .con())
}
\arguments{
\item{assay}{Character vector of assay terms to search for, e.g.
\code{c("scRNA-seq", "snRNA-seq")}.}

\item{year_from, year_to}{Optional integer. Restrict to projects published
within this year range.}

\item{con}{A \code{seqout_connection}. Defaults to the shared REST connection.}
}
\value{
A tibble with columns \code{accession}, \code{assay_type}, and \code{published}
(first day of the publication month). One row per unique
project–assay-type combination.
}
\description{
Retrieves GEO projects whose assay annotation contains the given term(s),
along with their publication dates. Matching is case-insensitive substring
search against ontology-annotated assay values (e.g., \code{"scRNA-seq"} matches
\code{scRNA-Seq}, \code{"snRNA-seq"} matches \code{snRNA-Seq}, \code{"ATAC-seq"} matches
\code{ATAC-Seq}).
}
\examples{
\dontrun{
con <- seqout_connect("parquet")

projects <- assay_projects(
  assay = c("scRNA-seq", "snRNA-seq"),
  year_from = 2015
)

library(dplyr)
projects |>
  mutate(year = as.integer(format(published, "\%Y"))) |>
  count(assay_type, year)
}
}
