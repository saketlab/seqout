% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/longread.R, R/zzz-aliases.R
\name{project_longread_chemistry}
\alias{project_longread_chemistry}
\alias{ProjectLongreadChemistry}
\title{Get PacBio/Oxford Nanopore chemistry calls for a study}
\usage{
project_longread_chemistry(accession, con = .con())

ProjectLongreadChemistry(accession, con = .con())
}
\arguments{
\item{accession}{A study or series accession, or one resolving to a study.}

\item{con}{A \code{seqout_connection}. Defaults to the shared REST connection;
a Parquet connection reads the dump.}
}
\value{
A run tibble, empty when the study has no long-read runs.
}
\description{
Every PacBio/Oxford Nanopore run in the study, independent of single-cell
status and Pentimento linkage. \code{chemistry_confidence} is \code{exact} when read
from the submitted BAM's own header, \code{declared} (ONT only) when parsed from
the depositor's own protocol text, \code{bucket} when inferred from
\code{instrument_model} alone, \code{unknown} otherwise.
}
\examples{
\dontrun{
ProjectLongreadChemistry("GSE297547")
}
}
\seealso{
\code{\link[=project_single_cell]{project_single_cell()}}, whose \code{longread_chemistry} attribute
answers the same question restricted to the study's first page of
samples.
}
