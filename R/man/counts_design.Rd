% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/counts.R, R/zzz-aliases.R
\name{counts_design}
\alias{counts_design}
\alias{CountsDesign}
\title{The study's sample table, one row per sample}
\usage{
counts_design(counts)

CountsDesign(counts)
}
\arguments{
\item{counts}{A \code{seqout_counts} tibble from \code{\link[=seqout_counts]{seqout_counts()}}.}
}
\value{
A tibble, one row per sample of the study.
}
\description{
The submitter's characteristics spread into one column each. Reuses the
sample record \code{\link[=seqout_counts]{seqout_counts()}} fetched.
}
\examples{
\dontrun{
counts <- SeqoutListCounts("GSE135251")
col_data <- CountsDesign(counts)
}
}
\seealso{
\code{\link[=counts_samples]{counts_samples()}} for the harmonised cohort fields.
}
