% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R, R/zzz-aliases.R
\name{download_bams}
\alias{download_bams}
\alias{DownloadBams}
\title{Download the alignment files a submitter sent}
\usage{
download_bams(accession, dest_dir = NULL, quiet = FALSE, con = .con())

DownloadBams(accession, dest_dir = NULL, quiet = FALSE, con = .con())
}
\arguments{
\item{accession}{Any accession Seqout holds. Resolved to its study, since
the archive files alignments against one.}

\item{dest_dir}{Directory to write into. Defaults to the accession.}

\item{quiet}{Suppress progress messages.}

\item{con}{A \code{seqout_connection}. Defaults to the shared REST connection.}
}
\value{
The paths of the downloaded files, invisibly.
}
\description{
Distinct from \code{\link[=download_runs]{download_runs()}}, which fetches reads. These are the
submitter's own BAMs, aligned to a reference they chose and often carrying
work the reads alone do not reconstruct: barcode tags, methylation calls,
long-read structural evidence. Realigning from fastq gives you neither those
nor their coordinates.
}
\details{
Most are held in requester-pays storage, which no anonymous client can read.
Those are named rather than fetched, with the command that would get them.
}
\examples{
\dontrun{
SeqoutGet("SRP071083")$bams # 276 files, 410 GB, all requester-pays
DownloadBams("SRP071083")
}
}
\seealso{
\code{\link[=download_runs]{download_runs()}} for the reads, and \code{seqout_get(x)$bams} to see
what exists before fetching any of it.
}
