% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort.R, R/zzz-aliases.R
\name{microbe_detections}
\alias{microbe_detections}
\alias{MicrobeDetections}
\title{The microbial detections behind a cohort, one row each}
\usage{
microbe_detections(
  x,
  validated_only = FALSE,
  columns = c("organism", "class", "kingdom", "breadth_frac", "kmer_mass")
)

MicrobeDetections(
  x,
  validated_only = FALSE,
  columns = c("organism", "class", "kingdom", "breadth_frac", "kmer_mass")
)
}
\arguments{
\item{x}{A sample tibble from \code{\link[=sample_search]{sample_search()}}, carrying \code{microbes}.}

\item{validated_only}{Keep only detections that pass the stored gates.}

\item{columns}{Detection fields to keep. The default set covers the organism,
its class and the two evidence measures.}
}
\value{
A tibble of \code{sample} plus the requested detection fields, empty when
nothing was detected.
}
\description{
Flattens the \code{microbes} list column from \code{\link[=sample_search]{sample_search()}} into one row
per (sample, organism, run).
}
\examples{
\dontrun{
hpv <- SampleSearch(disease_ontology_id = "MONDO:0002974", microbe = "HPV")
d <- MicrobeDetections(hpv)
sort(table(d$organism), decreasing = TRUE)

# only the detections that pass the gates
MicrobeDetections(hpv, validated_only = TRUE)
}
}
\seealso{
\code{\link[=sample_search]{sample_search()}} for the samples, \code{\link[=sample_microbes]{sample_microbes()}} for every
detection in one sample.
}
