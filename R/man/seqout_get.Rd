% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R, R/zzz-aliases.R
\name{seqout_get}
\alias{seqout_get}
\alias{SeqoutGet}
\title{Everything reachable from one accession}
\usage{
seqout_get(accession, con = .con())

SeqoutGet(accession, con = .con())
}
\arguments{
\item{accession}{Any accession Seqout holds.}

\item{con}{A \code{seqout_connection}. Defaults to the shared REST connection.}
}
\value{
A \code{seqout_dataset} object with lazy fields.
}
\description{
Lazy fields fetch on first access and then cache their result.
}
\details{
The fields are:

\describe{
\item{\code{meta}}{the project record: title, summary, design, dates, files}
\item{\code{samples}}{one record per sample}
\item{\code{experiments}}{one record per library preparation}
\item{\code{runs}}{one record per sequencing run, with its file URLs}
\item{\code{supplementary}}{one row per supplementary file, series and sample
alike; \code{sample} is \code{NA} on the ones the series carries itself}
\item{\code{bams}}{the alignment files a submitter sent, where there are any}
\item{\code{pubs}}{the publications linked to the dataset}
\item{\code{links}}{the same data in other archives}
\item{\code{enriched}}{the harmonised data for the samples: the submitter's free
text read into one vocabulary, with ontology IDs}
\item{\code{detail}}{the record for a sample or run accession}
}
}
\examples{
\dontrun{
d <- SeqoutGet("GSE168652")
d$meta$title
nrow(d$samples)
nrow(d$runs)
}
}
