% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/counts.R, R/zzz-aliases.R
\name{counts_samples}
\alias{counts_samples}
\alias{CountsSamples}
\title{Choose which samples of a study to read}
\usage{
counts_samples(counts, ..., min_cell_count = 1L)

CountsSamples(counts, ..., min_cell_count = 1L)
}
\arguments{
\item{counts}{A \code{seqout_counts} tibble from \code{\link[=seqout_counts]{seqout_counts()}}, built on a
GSE.}

\item{...}{Filters for \code{\link[=sample_search]{sample_search()}}, by name, such as
\code{tissue = "liver"}. \code{study_accession} comes from \code{counts}.}

\item{min_cell_count}{Smallest cell count to keep. Samples with no recorded
count go too; \code{NULL} keeps everything.}
}
\value{
A tibble of the matching samples, sorted by highest cells first,
with the \code{unit}  and \code{format} \code{\link[=seqout_matrix]{seqout_matrix()}} would read.
}
\description{
Filters a study's samples through \code{\link[=sample_search]{sample_search()}} and keeps the ones that
have a readable unit. Use it on a series that mixes tissues or assays.
}
\examples{
\dontrun{
counts <- seqout_counts("GSE182159")
liver <- counts_samples(counts, tissue = "liver", min_cell_count = 1000)
m <- seqout_matrix(counts, sample = liver$unit[1])
}
}
\seealso{
\code{\link[=seqout_counts]{seqout_counts()}} for every unit, unfiltered.
}
