% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ontology.R, R/zzz-aliases.R
\name{map_to_ontology}
\alias{map_to_ontology}
\alias{MapToOntology}
\title{Map a column of labels to ontology identifiers}
\usage{
map_to_ontology(
  x,
  columns,
  ontology = NULL,
  use_synonyms = FALSE,
  max_hops = 1,
  con = .con()
)

MapToOntology(
  x,
  columns,
  ontology = NULL,
  use_synonyms = FALSE,
  max_hops = 1,
  con = .con()
)
}
\arguments{
\item{x}{A data frame to copy and annotate.}

\item{columns}{Character. The name of one column, or of several.}

\item{ontology}{Keep the identifiers of one source only, for example \code{"CL"}
for cell types or \code{"UBERON"} for anatomy. The default keeps all of them.}

\item{use_synonyms}{Let a label with no identifier of its own take the
identifiers of its synonyms. \code{FALSE} by default.}

\item{max_hops}{How far to walk the synonym links, 1 to 4. Read only when
\code{use_synonyms} is \code{TRUE}.}

\item{con}{A \code{seqout_connection}. Defaults to the shared REST connection.}
}
\value{
\code{x} with one new character column for each column that you named.
}
\description{
Adds \verb{<column>_ontology_id} columns with comma-separated CURIEs.
Missing or empty labels become \code{NA}.
}
\details{
Only the identifiers of the label itself are read. Under \code{use_synonyms},
synonym IDs can be narrower; own IDs win.

Reads REST. The ontology graph is absent from the dump.
}
\examples{
\dontrun{
meta <- data.frame(celltype = c("T cell", "hepatocyte", "HeLa"))
MapToOntology(meta, "celltype")

# Cell Ontology IDs
MapToOntology(meta, "celltype", ontology = "CL")

# allow synonym IDs
MapToOntology(meta, "celltype", use_synonyms = TRUE)
}
}
\seealso{
\code{\link[=ontology]{ontology()}} for what the graph holds about one term.
}
